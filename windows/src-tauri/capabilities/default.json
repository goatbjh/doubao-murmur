{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local pages only. The login window loads doubao.com and is deliberately left out: it has no need for the Tauri bridge, since credentials are handed to the host through cookies.",
  "windows": ["overlay", "help"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
